# Issue #59 `300114.SZ` → `302132.SZ` same-provider continuity probe（2026-09-15）

状态：`STOP(BLOCKED) / CURRENT-CODE IDENTITY FOUND / HISTORICAL CONTINUITY NOT PROVEN`

本记录承接项目经理对 PR #71 的 review `5217204028` 及 Issue #59 scheduler comment
`5690045901`。最新任务要求仍限定在 Owner 批准的 AmazingData，并且只调查这一发行人的
当前代码 `302132.SZ` 与历史代码 `300114.SZ` 是否存在 provider-owned 连续性证据。

## 范围

- AmazingData `1.1.9`、tgw `1.0.9.2`。
- 本轮实际请求的唯一代码是 `["302132.SZ"]`；没有查询其他证券或其他 provider。
- 只读取/检查已安装 SDK 的 public query surface 和底层类型；没有调用无界的证券类型全量查询。
- 原始返回值仅保留在被忽略的 `data/spike/identity_probe_302132_SZ_20260915/`，GitHub 只提交本脱敏记录。

## 实际 provider 结果

| 入口 | 状态 | 形状 | 行数 | provider-owned 字段值 |
|---|---|---|---:|---|
| `AmazingDataProvider.get_stock_basic_exchange` → `InfoData.get_stock_basic` | `OK` | `pandas.DataFrame`、`RangeIndex` | 1 | `MARKET_CODE=302132.SZ`；`LISTDATE=20100827`；`DELISTDATE=null`；`IS_LISTED=1` |
| `DownloadInfoData.download_stock_basic` | `OK` | `pandas.DataFrame`、`RangeIndex` | 1 | 同上 |

两条入口返回的列均为：

`MARKET_CODE, SECURITY_NAME, COMP_NAME, PINYIN, COMP_NAME_ENG, LISTDATE, DELISTDATE, LISTPLATE_NAME, COMP_SNAME_ENG, IS_LISTED`

typed facade 的脱敏 raw anchor：request `114a9eb6-6ddb-4507-9e28-0df779ee4469`、
payload SHA-256 `895311966dfbfe8f873c35aeed3399bb9b3668855afd64fa0f141e74c08629cd`、
evidence SHA-256 `2e1cf312ea24895c41574dd0512ba90204c9f021a9721d1f2aa13a1d98d517ea`。

底层入口的本地忽略 raw artifact 为
`data/spike/identity_probe_302132_SZ_20260915/raw/direct_download_stock_basic.parquet`，
大小 `7,239` bytes，SHA-256
`0cd1b34ebb80c344d3d0f0973505d8b4aebd470e925f1bb370fbdb187674e858`。

## SDK / provider surface 检查

已安装 SDK 的可见入口包括：

- `InfoData.get_stock_basic(code_list)` 及其底层 `DownloadInfoData.download_stock_basic(code_list)`；
- `BaseData.get_code_list(security_type)`、`BaseData.get_hist_code_list(security_type, start_date, end_date)`，二者是 membership surface；
- `BaseData.get_code_info(security_type)`，签名只有 `security_type`，静态字节码显示它按
  `kSZSE/kSSE/kNEEQ` 构造无界 `QuerySecuritiesInfo` 结果，再暴露 `symbol`、
  `security_code`、`list_day` 等当前代码表字段；本轮没有调用它；
- `InfoData.get_bj_code_mapping` 是另一个交易所映射名称，但 inspected public surface 没有
  暴露 A 股旧代码/新代码/有效区间的关系；本轮没有把它当作 A 股连续性证据。

底层 `tgw` 可见的 `SecurityInfo`、`MDStockInfo`、`MDCodeTable`/`MDCodeTableRecord`
字段提供当前 `security_code`/`symbol`、`list_day` 等字段，但没有发现同时绑定
`300114.SZ` 与 `302132.SZ` 的 old/new code、alias、security-id continuity 或 effective
interval carrier。`download_stock_basic` 的字节码也确认它只遍历调用方传入的 code list，
逐个把精确值作为 `market_code` 请求；不会自行发现历史代码关系。

## 判定

`302132.SZ` 的当前主数据和上市日期已由同一 provider 明确返回；但是这条记录没有出现
`300114.SZ`、旧新代码关系、稳定跨代码 security identity 或代码切换有效区间。因此它
不能单独证明 `300114.SZ` 与 `302132.SZ` 是同一证券，也不能直接作为 2024-01 的历史 PIT
identity mapping。

当前严格结论为：`STOP(BLOCKED)`。

本轮没有：

- 将 `300114.SZ` 映射或替换为 `302132.SZ`；
- 从名称、代码数字相似性、行情、查询日期、请求顺序或 membership 推断连续性；
- 修改 5,106-member universe 或运行时代码；
- 重跑完整 Canonical → Snapshot → ReadModel → projection，或继续 acquisition、receipt、coverage、materialization；
- 使用其他 provider、公共网页事实或通用 alias framework。

项目经理在 review 中引用的公开市场材料只改变了调查假设，未被作为运行时输入或本轮
provider-owned continuity evidence。

## 最小解除条件

需要 AmazingData 自身提供能绑定这两个精确代码的 provider-owned 关系（例如同一稳定
security identity 加 old/new code 与有效日/区间），且该关系可用于 2024-01 PIT 判断。
满足后才提出最小窄范围映射并重跑既有完整链路；在此之前保持 `STOP(BLOCKED)`。

账号、密码、IP、端口、完整 raw payload、SDK/runtime 文件和 DuckDB 均未进入 Git。
