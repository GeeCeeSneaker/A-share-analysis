# 开发工作要求：初始化并持续维护项目《开发管理总册》

> 项目：`GeeCeeSneaker/A-share-analysis`  
> 工作类型：Documentation Governance / Project Management  
> 优先级：P0（治理要求，不改变 Frozen Baseline）  
> 下达日期：2026-08-22  
> 本次交付文件：`DEVELOPMENT_MANAGEMENT.md`

---

# 1. 工作目标

从本批次开始，在仓库建立一个长期、固定路径、持续维护的项目开发管理总册，用于统一记录：

```text
系统设计当前方案
Frozen Baseline 与 ADR 关系
数据架构与关键契约
Provider / PIT / Security Identity / Source Policy
Storage / Snapshot / Artifact / Publish / Replay
Feature / State 设计摘要
开发阶段与里程碑
Entry / Exit Gates
风险与技术债
当前阻塞项
测试 / CI / 安全治理
设计变更记录
下一阶段工作
```

该文件与 `docs/DEVLOG.md` 分工如下：

```text
DEVLOG
    记录“发生过什么”

DEVELOPMENT_MANAGEMENT
    记录“系统现在是什么、当前按什么规则继续开发”
```

---

# 2. 仓库固定存储路径

必须创建目录：

```text
docs/project/
```

并将随本工作要求提供的管理文档上传为：

```text
docs/project/DEVELOPMENT_MANAGEMENT.md
```

**不得改名，不得放到 `docs/design/`，不得按日期创建副本。**

以后永远更新：

```text
docs/project/DEVELOPMENT_MANAGEMENT.md
```

过去版本由 Git 历史保存。

---

# 3. 本次必须完成的仓库修改

本次提交至少完成：

1. 新建：
   ```text
   docs/project/DEVELOPMENT_MANAGEMENT.md
   ```
2. 内容使用本工作要求附带的 `DEVELOPMENT_MANAGEMENT.md` 作为初始化版本；
3. 在：
   ```text
   docs/DEVLOG.md
   ```
   顶部增加本批次日志；
4. DEVLOG 条目写明：
   ```text
   Implementation Status = DONE
   Review Status = PENDING_REVIEW
   ```
5. 在管理总册首个 Change Log：
   ```text
   DM-CR-20260822-001
   ```
   中，将 `Commit: 待上传仓库后填写` 更新为本批次最终 Commit SHA；如提交前无法知道 SHA，可在紧邻的 docs-only commit 中回填；
6. 不修改 `A股市场态势数据基座_日频模块_V1.3.2_开发方案.md` 的 Frozen Baseline 内容。

---

# 4. 建议归档本工作要求

建议把本工作要求也归档到：

```text
docs/design/开发管理总册_初始化与持续维护工作要求_20260822.md
```

这不是运行时依赖，但有助于后续审计治理规则来源。

如团队决定不继续增加历史 Work Requirement 文件，可以不归档本要求；但管理总册必须按指定路径提交。

---

# 5. DEVLOG 条目建议模板

```markdown
## 2026-08-22 · 建立项目开发管理总册

**Scope**
- 初始化长期项目治理文档

**Implementation**
- 新建 docs/project/DEVELOPMENT_MANAGEMENT.md
- 固化 Design/Progress/Change-Control/Entry-Gate 管理规则
- 固化“设计变化必须同一逻辑提交更新管理总册”的要求

**Schema / Contract Changes**
- 无运行时代码/Schema 改动
- 新增 Documentation Governance Contract

**Verification**
- 文档路径检查
- Markdown 可读性检查
- 与 V1.3.2 / DEVLOG / 当前 R4 状态核对

**Implementation Status**
- DONE

**Review Status**
- PENDING_REVIEW

**Next**
- R4-A1.1 + CR-1
```

---

# 6. 从本提交开始的永久规则

以后普通代码提交：

```text
必须更新 docs/DEVLOG.md
```

以后只要发生设计/契约变化：

```text
必须同时更新：
docs/DEVLOG.md
docs/project/DEVELOPMENT_MANAGEMENT.md
```

设计/契约变化包括但不限于：

```text
Architecture
Provider Endpoint / Capability / Runtime
Schema / Migration
Security Identity
Universe
Trading Rule
PIT / available_at
Source Policy
Raw / Canonical Contract
Snapshot / Artifact / Publish / Replay
Feature Formula
State / Regime
Golden Truth / Spike Gate
Capability Approval
CI / Reproducibility Contract
Concurrency Ownership
Security / Secrets
Milestone Entry / Exit Gate
```

---

# 7. 后续工作要求执行规则

以后如果新的开发工作要求包含设计调整，开发人员必须在任务中明确：

```text
Documentation Update Required: YES

Target:
docs/project/DEVELOPMENT_MANAGEMENT.md

Change ID:
DM-CR-YYYYMMDD-NNN

Affected Sections:
<列出章节>
```

实现时必须同一逻辑批次提交：

```text
Code
Tests
DEVLOG
DEVELOPMENT_MANAGEMENT
ADR（如需要）
Migration（如需要）
```

不得先合代码、以后再补管理文档。

---

# 8. 设计变更分级

按管理总册执行：

```text
C0 Implementation Only
C1 Contract Clarification
C2 Design Change
C3 Frozen Baseline Change
```

## C0

要求：

```text
DEVLOG + Tests
```

## C1

要求：

```text
DEVLOG
DEVELOPMENT_MANAGEMENT
Contract Tests
```

## C2

要求：

```text
DEVLOG
DEVELOPMENT_MANAGEMENT
ADR
Tests
Migration / Compatibility Plan（如适用）
```

## C3

必须：

```text
Project Owner 明确批准
新 Frozen Baseline Version
Impact Analysis
ADR
Migration / Backfill Plan
Regression Plan
```

---

# 9. 与现有文档的关系

不得删除或替代：

```text
docs/design/A股市场态势数据基座_日频模块_V1.3.2_开发方案.md
docs/DEVLOG.md
docs/adr/
docs/provider_verification/
docs/risk_register.md
docs/spike_report_p0m1.md
```

新的管理总册是它们的“当前状态中枢”和索引，不是把 Frozen Baseline 全量复制一遍。

---

# 10. 初始化状态必须保持真实

上传时管理总册中的当前状态应保持为：

```text
P0-M0
    PASS

R4-A1
    Implementation DONE
    Review REOPENED

R4-A1.1
    READY / highest priority

R4-A2/A3/B1/B2/CI
    PLANNED

CR-1
    READY，允许并行启动

Production P0-M-1B
    BLOCKED by R4 + official account

Real P0a
    BLOCKED by Provider eligibility + Canonical Runtime
```

如果上传管理总册之前仓库又有新代码，必须按最新 HEAD 更新状态，不得机械提交旧状态。

---

# 11. 当前下一阶段开发顺序

管理总册初始化后继续：

```text
Track A
R4-A1.1 Truth Integrity
→ R4-A2
→ R4-A3
→ R4-B1
→ R4-B2
→ R4-CI

Track B（可并行）
CR-1 ProviderExchange + RawWriter
```

本次初始化提交不要把大批业务代码混入同一个 Commit。

---

# 12. 建议新增 CI Governance Gate

可本次一起做，也可下一紧邻提交做。

## DEVLOG Gate 扩展

当 commit 修改：

```text
src/
migrations/
configs/
scripts/
data/golden/
.gitattributes
.github/workflows/
```

必须修改：

```text
docs/DEVLOG.md
```

## Development Management Gate

当 commit 明显修改设计/契约，例如：

```text
migrations/
docs/adr/
data/golden/
Canonical / Publish / Identity / SourcePolicy Contract
```

应要求：

```text
docs/project/DEVELOPMENT_MANAGEMENT.md
```

注意：不要简单规定所有 `src/` bugfix 都必须改管理总册，以免产生无意义文档噪音。可以先用 PR Checklist / Commit Convention 管理设计变更触发条件，再逐步自动化。

---

# 13. Change Log 规则

每次 C1/C2/C3 更新必须新增：

```text
Change ID
Type
Date
Status
Trigger
Old Contract
New Contract
Reason
Affected Modules
Affected Data
Compatibility
Migration / Backfill
Tests
ADR
Commit
Reviewer
```

不得覆盖历史 Change Entry。

---

# 14. Review Status 规则

开发人员完成实现后只能写：

```text
Implementation Status = DONE
Review Status = PENDING_REVIEW
```

不得自行写：

```text
VERIFIED
```

如果复核发现问题：

```text
Review Status = REOPENED
```

---

# 15. Frozen Baseline 规则

本工作不改变：

```text
V1.3.2 Frozen Baseline
```

后续如果工作要求触及：

```text
核心分层
PIT 核心原则
Feature 核心数学定义
State 核心语义
Security Identity 核心定义
```

必须按 C3 处理：

```text
Project Owner 批准
新 Baseline
ADR
影响分析
```

不能直接改旧 Frozen Baseline。

---

# 16. 本次验收标准

```text
[ ] docs/project/DEVELOPMENT_MANAGEMENT.md 已存在
[ ] 路径和文件名完全一致
[ ] 内容与初始化版本一致，或仅按最新 HEAD 做必要同步
[ ] docs/DEVLOG.md 已新增本批次条目
[ ] DM-CR-20260822-001 已记录
[ ] Frozen Baseline 未被修改
[ ] 无 credential / token / wheel / private provider data 进入 Git
[ ] Markdown 路径有效
[ ] git diff 仅包含治理初始化所需内容
```

建议 Commit Message：

```text
docs: establish project development management governance
```

---

# 17. 提交后回报格式

开发完成后请回报：

```text
Commit SHA:
Files Added:
Files Modified:
Management Doc Path:
DEVLOG Entry:
CI / Tests:
Known Issues:
Implementation Status:
Review Status:
```

必须明确：

```text
Management Doc Path:
docs/project/DEVELOPMENT_MANAGEMENT.md
```

---

# 18. 永久治理要求

从此之后：

> 只要新的开发工作要求导致系统设计方案调整，开发人员必须在完成代码的同时更新 `docs/project/DEVELOPMENT_MANAGEMENT.md` 并提交 GitHub。

文档职责：

```text
docs/DEVLOG.md
    时间线

docs/project/DEVELOPMENT_MANAGEMENT.md
    当前系统真相 + 状态 + 变更控制

docs/adr/
    重大设计决策理由

Frozen Baseline
    稳定核心定义

Git
    历史版本与责任链
```

该要求不是一次性任务，而是项目长期开发治理契约。
